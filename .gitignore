# Données : symlink vers le dossier Google Drive partagé (hors repo)
/data

# Outillage local (suivi d'enjeux Beads) — non versionné
.beads/
AGENTS.md

eval/

# App de pricing locale — non versionnée, SAUF la doc de coût (price cards,
# variables à mesurer) qui est une référence partagée (epic 97r).
pricing_app/*
!pricing_app/*.md
# Outillage de mesure du coût marginal : versionné pour que les chiffres soient
# reproductibles. `app.py` (le Streamlit local) reste ignoré.
!pricing_app/aggregate_costlog.py
!pricing_app/run_instrumented.py

# Secrets / env
*.env
aws_keys.env

# Python
.venv*/
__pycache__/
*.py[cod]

# Node
node_modules/

# Vite build output
dist/

# TypeScript incremental build info
*.tsbuildinfo

# Bases locales
*.sqlite
*.db
*.npy

# OS / éditeurs
.DS_Store
*.log

# Local Netlify folder
.netlify

# Bundles de fonctions pré-packagés (ne jamais committer : ils shadowent le .ts)
netlify/functions/*.zip

# Worktrees éphémères créés par Claude Code
.claude/worktrees/
